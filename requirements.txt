# Install ALL four backends at once (base deps + every backend's pip extras). For a single
# backend, use the smaller requirements/<backend>.txt instead.
#
# Beyond pip, each backend also needs its model code + weights (see README "Setup"):
#   * pairvpr  -> Pair-VPR/ git submodule + vitG checkpoint (kept outside the repo)
#   * edtformer-> clone EDTformer/ ; weights auto-download from torch.hub
#   * fol      -> nothing (model + backbone auto-download from torch.hub)
#   * unipr3d  -> clone UniPR-3D/ ; single_model.ckpt auto-downloads from HF
#
# IMPORTANT (GPU): on an RTX 5090 (Blackwell / sm_120) install torch first from the cu128 index --
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
-r requirements/base.txt
omegaconf>=2.3        # pairvpr
timm                  # edtformer
huggingface_hub       # fol, unipr3d
loralib               # unipr3d
einops                # unipr3d
